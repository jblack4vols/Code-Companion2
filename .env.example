# Required
DATABASE_URL=
SESSION_SECRET=

# Optional
# SEED_OWNER_PASSWORD=
# SEED_USER_PASSWORD=
# PORT=5000

# Microsoft 365 / Azure AD (SSO + Outlook calendar + SharePoint)
# Azure AD app registration: debda2f0-a35b-44c9-8e0b-9d1d306c49a8
# Tenant: 668d2c67-481c-4c6c-8904-b08dfd68308c
# Required redirect URIs (register all in Azure -> App registrations -> Authentication):
#   https://crm.tristarpt.com/api/auth/microsoft/callback   (SSO)
#   https://crm.tristarpt.com/api/outlook/callback          (Outlook calendar)
#   https://crm.tristarpt.com/api/sharepoint/callback       (SharePoint)
#   http://localhost:5000/api/auth/microsoft/callback       (local dev SSO)
#   http://localhost:5000/api/outlook/callback              (local dev Outlook)
#   http://localhost:5000/api/sharepoint/callback           (local dev SharePoint)
# Required API permissions (delegated):
#   openid, profile, email, User.Read, Calendars.ReadWrite,
#   Sites.ReadWrite.All, Files.ReadWrite.All, offline_access
# AZURE_AD_CLIENT_ID=
# AZURE_AD_TENANT_ID=
# AZURE_AD_CLIENT_SECRET=

# SMTP (server-side email - task assignments, digests, reset emails)
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
